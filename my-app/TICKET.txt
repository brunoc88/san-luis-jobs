Objetivos

- PENDIENTE:
    - ADMINPANEL
        
        USER:
        GET /(LIST)
        - LISTADO DE CUENTAS ACTIVAS y NO ACTIVAS(INCATIVAS SOLO PARA TODOS, INCLUIR LAS ACTIVAS PARA 
        EL SUPERADMIN PUEDA ADEMAS DE ACTIVARLAS COMO LO PUEDE HACER UN ADMIN AQUI EL SUPERADMIN PODRA
        DARLE O QUITARLE AL USUARIO EL ROL DE ADMIN) 

        PATCH /ID(ACTIVATE AN INACTIVE AND NOT SUSPENDED ACCOUNT)
        (notificar con email que se activo su cuenta)
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR
        
        SUPERADMIN
        PATCH /ID/GIVEROLE(GIVE OR LOOSE ADMIN ROLE)
        (notificar con email que recibio rol de admin)
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR
        

        /GET(LIST ACCOUNT SUSPENDED)
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR

        PATCH /ID(ACTIVATE SUSPENDED ACCOUNT)
        (notificar con email que se activo su cuenta)
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR

        GET /ID/SUSPENSION-AUDIT(VER PUBLICACIONES SUSPENDIAS DE USUARIO SUSPENDIDO)
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR
        
    - USER
        /PROFILE:
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR
        /USERNAME:
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR
        /CONFIG:
        - ENDPOINT/SERVICE/REPO
        - TEST
        - DOCUMENTAR

    - USER:
        /ID/SUSPENSION-AUDIT
        (SE VAN A MOSTRAR TODOS LOS JOBS SUSPENDIDO)
        (NO VA A SER UNA LISTA, ASI A LO CRUDO)
            - UX(FIGMA)
            - FIGMA   
    - JOB:
        - EDITAR
            - UX(FIGMA)
            - FIGMA
            - ENDPOINT/SERVICE/REPO
            
    - LOCATION 
            /PANEL:
            - UX(FIGMA)
    
        


DOCUMENTAR:
    - nextauth
    - credentials-authorize
    - next-auth.d.ts
    - clodinary(uploadFile)
    - todos los repositorios




    